using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateTab : MonoBehaviour
{
    public int height;
    public int width;
    public int nbMine;

    // Start is called before the first frame update
    void Start()
    {
        GameObject.Find("GameData");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
